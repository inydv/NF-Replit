<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Template</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #f9f9f9;
        color: #333;
        line-height: 1.6;
      }

      main {
        background: #ffffff;
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        margin: 0 auto;
        text-align: left;
      }

      .main-p {
        margin: 0 0 1em 0;
        font-size: 16px;
      }

      .highlight {
        font-weight: bold;
        color: #0056b3;
      }

      .line {
        height: 1px;
        background-color: #ddd;
        margin: 20px 0;
      }

      footer {
        text-align: center;
        font-size: 14px;
        color: #777;
        margin-top: 20px;
      }

      footer a {
        color: #0056b3;
        text-decoration: none;
      }

      footer a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <main>
      <p class="main-p">Hello Francis</p>
      <p class="main-p">
        <%= Message %>
        <br />
        <br />
        <span class="highlight">Name:</span> <%= Name %>
        <br />
        <span class="highlight">Contact:</span> <%= Email %>
      </p>
    </main>

    <div class="line"></div>

    <footer>
      This message is from
      <a href="https://nursingfront.com/contact">NursingFront ContactUs Page</a
      >.
    </footer>
  </body>
</html>
